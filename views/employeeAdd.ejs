<%- include('includes/_header.ejs') %> 
<div class="flex">
  <aside class="w-64">
    <%- include('includes/_navbar') %>
  </aside>
<div class="content p-4">
  <div class="card bg-white shadow-md rounded-lg p-4">
    <div class="card-content">
      <h1 class="text-3xl font-semibold mb-4">Agregar empleado</h1>
      <p class="text-lg mb-6">
        En esta vista, el super admin podrá agregar a nuevos empleados.
      </p>
      <!-- Aquí puedes agregar el formulario de alta de empleado -->
      <form action="/employee/add" method="POST">
        <div class="space-y-4">
          <div class="field">
            <label for="employeeName" class="block text-lg font-medium"
              >Nombre del empleado</label
            >
            <input
              type="text"
              id="employeeName"
              name="employeeName"
              class="input bg-gray-100 border border-gray-300 p-3 rounded-md w-full"
              placeholder="Nombre completo"
              required
            />
          </div>
          <div class="field">
            <label for="employeeEmail" class="block text-lg font-medium"
              >Correo del empleado</label
            >
            <input
              type="email"
              id="employeeEmail"
              name="employeeEmail"
              class="input bg-gray-100 border border-gray-300 p-3 rounded-md w-full"
              placeholder="correo@ejemplo.com"
              required
            />
          </div>
          <div class="field">
            <label for="employeeRole" class="block text-lg font-medium"
              >Rol del empleado</label
            >
            <input
              type="text"
              id="employeeRole"
              name="employeeRole"
              class="input bg-gray-100 border border-gray-300 p-3 rounded-md w-full"
              placeholder="Rol del empleado"
              required
            />
          </div>
          <div class="flex justify-end mt-4">
            <button
              type="submit"
              class="btn bg-blue-500 text-white px-6 py-2 rounded-md hover:bg-blue-600"
            >
              Agregar
            </button>
            <a
              href="/employee"
              class="btn bg-gray-500 text-white px-6 py-2 rounded-md hover:bg-gray-600 ml-4"
              >Cancelar</a
            >
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
</div>

<%- include('includes/_footer.ejs') %>
