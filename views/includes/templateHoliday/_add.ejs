<script>
    addBackButton("/holiday");
</script>
<div class="primaryContainer bgContainer1">
    <div class="titleContainer">
        <h2 class="title text-center flex-1">Add Template Holiday</h2>
    </div>

    <form action="/holiday/template/add" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <div class="flex space-x-6 mb-4">
            <div class="field w-full">
                <label for="title" class="block text-lg font-medium">
                    Select Holiday Name
                </label>
                <input
                    type="text"
                    id="title"
                    name="title"
                    class="input bg-neutral-700/50 border border-neutral-600/50 p-3 rounded-md w-full"
                    placeholder="Dia de la Bandera"
                    required
                />
            </div>

            <div class="field w-1/2">
                <label for="holidayDate" class="block text-lg font-medium">
                    Celebration Date
                </label>
                <input
                    type="date"
                    id="holidayDate"
                    name="holidayDate"
                    class="input bg-neutral-700/50 border border-neutral-600/50 p-3 rounded-md w-full"
                    required
                />
            </div>
        </div>

        <div class="flex justify-end mt-4">
            <button type="submit" class="btnPrimary">Add</button>
        </div>
    </form>
</div>
